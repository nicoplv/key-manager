{% extends "templates/main.twig" %}

{% block body %}
<div class="large-12 columns">
	<div class="callout panel text-center">
		<h1>{{ message }}</h1>
	</div>
	{% if goback is defined %}
	<a href="{{ goback }}" class="button">Back</a>
	{% endif %}
</div>
{% endblock %}